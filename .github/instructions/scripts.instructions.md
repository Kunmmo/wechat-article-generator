---
applyTo: "scripts/**/*.py"
---

# Python Scripts Instructions

When editing Python scripts:

- Follow PEP 8 style guide
- Add type hints for all function parameters
- Handle errors gracefully with try/except
- Use `config/gemini.json` for API configuration (never hardcode keys)
- Log important operations using print or logging module
